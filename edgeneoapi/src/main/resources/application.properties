# Perfil ativo e nome da aplicação
spring.profiles.active=@spring.profiles.active@
spring.application.name=edgeneoapi

# Configuração do servidor
server.port=9000
spring.output.ansi.enabled=always

# Configurações JWT
jwt.secret=webacademy

# Configuração do JPA/Hibernate/ORM
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Inicialização do banco de dados com scripts SQL
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
spring.sql.init.encoding=UTF-8
spring.sql.init.platform=mysql
spring.sql.init.data-locations=classpath:data.sql

# Configuração do Redis
spring.redis.host=localhost
spring.redis.port=6379

# Configurações de upload de arquivos (opcional - descomentado)
# file.update-dir=uploads
# spring.servlet.multipart.max-file-size=20MB
# spring.servlet.multipart.max-request-size=20MB

# Configurações de e-mail para Spring Mail (usando Gmail como exemplo)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=edgeneoprevisao@gmail.com
spring.mail.password=sbak rwww qgtf wtwz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Google Login
spring.security.oauth2.client.registration.google.client-id=
spring.security.oauth2.client.registration.google.client-secret=
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:9000/login/oauth2/code/google
spring.security.oauth2.client.registration.google.client-name=Google
# spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
# spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
# spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
# GitHub Login

# Nível de log para Spring Security
logging.level.org.springframework.security=TRACE    
